<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flyer Upload</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: #f5f5f5;
    }
    .flyer {
      position: relative;
      width: 400px;
      height: 600px;
      margin: 20px auto;
      background: url('flyer-bg.jpg') no-repeat center/cover; /* replace with your flyer background */
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    }
    .circle-mask {
      position: absolute;
      top: 50px;
      left: 50%;
      transform: translateX(-50%);
      width: 200px;
      height: 200px;
      border-radius: 50%;
      overflow: hidden;
      border: 5px solid white;
      background: #ccc;
    }
    .circle-mask img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    input[type=file] {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <h2>Upload Your Image into the Flyer</h2>
  <div class="flyer">
    <div class="circle-mask" id="circle">
      <!-- uploaded image will appear here -->
    </div>
  </div>
  <input type="file" accept="image/*" id="upload">

  <script>
    const upload = document.getElementById('upload');
    const circle = document.getElementById('circle');

    upload.addEventListener('change', function(e) {
      const file = e.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = function(evt) {
          circle.innerHTML = `<img src="${evt.target.result}" alt="Uploaded Image">`;
        }
        reader.readAsDataURL(file);
      }
    });
  </script>
</body>
</html>
